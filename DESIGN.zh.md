# DESIGN — 技术说明与设计思路

> 本文件是「占坑」的定调文档：说清**主张什么、为什么新、怎么落地、怎么验证**。后续所有公开动作（README / 博客 / arXiv / 时间戳）都从这里派生。

---

## 1. 标题与一句话

**基于 DSH 时空组合性的具身机器人能力热插拔（ros-hotplug-by-dsh）**

将 DSH 的「时空组合性」用作机器人能力热插拔的组合原语：**空间上分层可见，时间上精确生灭**。

---

## 2. 摘要

- **问题**：具身机器人需要运行时增删/替换能力（换末端执行器、加传感器、升级技能），但现有方案要么绑定重启、要么只管硬件层、要么与 LLM agent 决策层脱节。
- **方法**：以 DSH 时空组合性（分层作用域 + Cordis 生命周期）作为**能力编排层**，叠加在 ROS2 之上；把每个机器人能力封装为 DSH 插件工具，热插拔 = 作用域注册/撤销。
- **结果**：实现并验证四项指标——**插入即见、拔出即回收、同名不串台、agent 无感**；并叠加华为式可靠性设计（校验、多版本、灰度、回滚、事件通知）。

---

## 3. 动机与背景

- 真实机器人在不同任务间要换末端执行器（夹爪 ↔ 吸盘）、加传感器、升级感知/技能；理想情况是**不重启、不停机、不影响其他能力**。
- 已有「可重构」工作多聚焦**硬件层或 ROS 组件层**，与上层「agent 决策」脱节；而 LLM/agent 机器人框架又多聚焦「单次任务」，缺少**能力热插拔的精确生命周期管理**。
- 因此需要一套「上层能力编排」原语：**挂载一个能力 = 立即可用；卸载 = 资源精确回收；同名能力互不串台；版本可灰度可回滚**。

---

## 4. 新颖性主张（核心 · 精确边界）

### 4.1 主张

> **率先将 DSH 的时空组合性（分层作用域 + Cordis 生命周期）应用于具身机器人能力的热插拔，并给出可复现实现与验证。**

### 4.2 不主张（边界）

| 明确不主张 | 原因 |
|---|---|
| 发明「时空组合性」 | 是 DSH（基于 Cordis + scope）的既有机制 |
| 第一个做「机器人热插拔」 | ROS2 生命周期/可组合节点、AICA、Eclipse Muto 等早已存在 |
| 第一个做「agent 控制 ROS」 | OpenRAL、RoboNeuron 等已有 |
| 硬件层/实时/电气安全热插拔 | 本项目只覆盖「软件能力层」 |

### 4.3 主张的实际落点

新颖性在于**这套机制 × 这个场景 × 这个实现**三者结合：
- 机制：DSH 的分层 scope + 父链继承 + nearest-wins + isolate realm + Cordis dispose + 版本时序（plugin/package/run）。
- 场景：具身机器人的末端执行器/传感器/技能热插拔。
- 实现：可复现的 `demo/13-hotplug`，含可靠性设计。

---

## 5. 近邻工作对比

| 工作 | 领域 | 与本文的区别 |
|---|---|---|
| ROS2 生命周期节点 / 可组合节点 | ROS 组件生命周期 | 只管理节点状态，不解决「agent 可见性 + 同型能力隔离 + 版本灰度回滚」 |
| AICA | 组件化可重构机器人框架 | 偏硬件/组件层，不含 LLM agent 编排层 |
| Eclipse Muto | 动态 ROS 软件栈编排 | 偏无人车场景与部署编排 |
| OpenRAL | ROS2 原生 agentic harness | 与本文最接近，但本文的独特锚点是「DSH 时空组合性」这套具体机制 |
| RoboNeuron | 基础模型 × ROS 模块化 | 偏模型接入，未强调热插拔的精确生命周期 |
| MCP | 通用工具协议 | 是协议标准，不含作用域/生命周期编排语义 |

> 结论：本文的差异化 = **「DSH 时空组合性」这一具体组合原语 + 「能力热插拔」这一具体场景 + 可靠性设计**。

---

## 6. 核心概念：DSH 时空组合性（简述）

详见 `docs/`（待建）。要点：

- **空间轴**：能力被分层分域摆放。Host 组合（进程级）vs agent preset 组合（每会话）；作用域父链（全局层 → preset 常驻层 → 每 agent 层），注册视图**向下继承、最近者胜**，事件放行**向上扩展**；`isolate` realm 隔离同型服务。
- **时间轴**：Cordis 插件 `apply → effect → dispose` 精确生灭；常驻挂载/世代；动态插件 plugin（实例）/ package（不可变版本）/ run（激活尝试）的版本时序与回滚。
- **锚点契约**：**注册上下文同时决定可见性与生命周期**——空间（谁看得见）与时间（谁生灭）由同一锚点绑定，杜绝「看得见却已死 / 活着却看不见」。

---

## 7. 系统设计

### 7.1 分层架构

```
[任务/自然语言指令]
        ↓
DSH Agent（决策编排）
        ↓ 工具调用
DSH Plugin（能力工具：grasp / suction / detect …）  ← 热插拔发生在这里
        ↓ rosbridge（WebSocket ↔ ROS2）
ROS2 控制层（C++ 控制节点 + Python 仿真桥）
        ↓
MuJoCo 仿真（Franka + 末端执行器 + 场景）
```

### 7.2 热插拔机制设计

| 操作 | 机制 | 效果 |
|---|---|---|
| 挂载能力 | 在 robot 作用域注册插件工具 | 立刻对 agent 可见可用 |
| 卸载能力 | dispose 该插件 | 精确回收其订阅/连接，agent 无感 |
| 替换能力 | 多版本共存 + `update` 灰度切换 | 新旧并存、平滑切换 |
| 同名隔离 | `isolate` realm | 两个夹爪/吸盘实例互不串台 |
| 失败回滚 | `run`(当前) vs `update`(目标) | 激活失败自动回滚旧版本 |
| 变化感知 | 事件广播 + agent 订阅 | agent 自动感知能力增删 |

---

## 8. 可靠性设计（华为经验 → 项目）

| 华为经验 | 本项目落点 | 验证方式 |
|---|---|---|
| 零信任安全流水线 | 能力挂载前 manifest / 哈希校验，不合法拒绝 | 传入篡改 manifest → 拒绝挂载 |
| 主备冗余 + 多版本共存 | 同一能力多版本共存 | 同时注册 v1/v2 不冲突 |
| 灰度升级 + 业务零中断 | `update` 灰度切换，agent 无感 | 切换期间任务不中断 |
| 异常秒级自动回滚 | 新能力激活失败回滚旧版 | 注入故障 → 自动回滚 |
| 发布/订阅事件通知 | 能力增删广播事件，agent 订阅感知 | 挂载/卸载时事件被收到 |
| 硬件差异屏蔽层 | 能力抽象层：同型末端同名遮蔽 | 两末端执行器同名遮蔽正确 |
| 高可用 / 资源不泄漏 | isolate 隔离 + dispose 精确回收 | 卸载后无残留连接/状态 |

> 这一节是把「华为经验」变成「可验证的项目能力」的关键，也是面试的差异化亮点。

---

## 9. 验证指标

| 指标 | 验收标准 |
|---|---|
| 插入即见 | 挂载后 agent 立即能调用新工具 |
| 拔出即回收 | 卸载后无残留订阅/连接（可观测到 teardown） |
| 同名不串台 | 两个同型能力注册不冲突、调用不串 |
| agent 无感切换 | 版本 `update` 期间任务成功率不下降 |
| 失败回滚 | 注入故障后自动回滚且旧能力仍可用 |

---

## 10. 局限与未来

- 只覆盖**软件能力层**热插拔；硬件层（电气/连接）、硬实时、安全边界不在本项目范围。
- 未来可扩展：真实硬件（ros2_control hardware_interface）、跨进程/跨机热插拔、与数据闭环/世界模型结合。

---

## 11. 披露与时间戳（占位）

> 待补：首次公开 commit hash、第三方时间戳服务回执、发布链接。

---

## 12. 参考文献

> 待补：DSH 文档、AICA、OpenRAL、RoboNeuron、ROS2 生命周期节点、软件可重构综述等。
